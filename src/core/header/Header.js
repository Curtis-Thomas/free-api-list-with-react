import { Box, Typography } from "@mui/material";

const Header = () => {
  return (
    <Box
      sx={{
        display: "flex",
        padding: "8px",
      }}
    >
      <Typography>Header</Typography>
    </Box>
  );
};

export default Header;
